# Миграция с Squid на Python Proxy

## Зачем мигрировать?

### Проблемы с Squid подходом
- ❌ Сложная конфигурация (`tcp_outgoing_address` или `fwmark`)
- ❌ IP адреса модемов динамические (меняются при переподключении)
- ❌ Нужна генерация конфига + reload
- ❌ Сложная отладка
- ❌ Специфичный синтаксис конфигов

### Преимущества Python Proxy
- ✅ **Явная привязка к интерфейсу** через `socket.bind(source_ip)`
- ✅ **Простая конфигурация** - понятный YAML
- ✅ **Легко расширять** - Python код легко читается и модифицируется
- ✅ **Прямая интеграция** с Management API
- ✅ **Hot reload** без сложностей
- ✅ **Полный контроль** над routing логикой

## План миграции

### Шаг 1: Установить Python proxy параллельно со Squid

```bash
ssh root@192.168.50.111

# 1. Обновить код
cd /root/repo/proxyfarm
git pull

# 2. Установить зависимости
pip3 install pyyaml

# 3. Создать конфиг
mkdir -p config
cp config/proxy.yaml config/proxy.yaml
nano config/proxy.yaml  # Настроить пользователей

# 4. Запустить на другом порту (для теста)
python3 proxy_full.py &  # Слушает на 3128

# Или временно запустить Squid на другом порту:
# В /etc/squid/squid.conf: http_port 3129
# squid -k reconfigure
```

### Шаг 2: Тестирование Python proxy

```bash
# Тест HTTP
curl -x http://user1:pass1@localhost:3128 http://ifconfig.me

# Тест HTTPS
curl -x http://user1:pass1@localhost:3128 https://ifconfig.me

# Тест балансировки
curl -x http://user1:pass1@localhost:3128 https://api.ipify.org  # wwan0
curl -x http://user2:pass2@localhost:3128 https://api.ipify.org  # wwan1

# Если все работает → переходим к Шагу 3
```

### Шаг 3: Остановить Squid и запустить Python proxy

```bash
# 1. Остановить Squid
systemctl stop squid
systemctl disable squid

# 2. Установить Python proxy как systemd service
cp /root/repo/proxyfarm/systemd/proxyfarm-proxy.service /etc/systemd/system/
systemctl daemon-reload
systemctl enable proxyfarm-proxy
systemctl start proxyfarm-proxy

# 3. Проверить статус
systemctl status proxyfarm-proxy

# 4. Проверить логи
journalctl -u proxyfarm-proxy -f
```

### Шаг 4: Обновить VPS (если используется)

На VPS ничего менять не нужно! socat продолжает работать:

```bash
# На VPS socat пробрасывает VPS:3128 → 10.8.0.2:3128
# Просто теперь на Orange Pi:3128 слушает Python proxy вместо Squid
```

### Шаг 5: Проверка с клиента

```bash
# С локальной машины через VPS
curl -x http://user1:pass1@138.2.138.243:3128 https://ifconfig.me
# Должен вернуть IP wwan0

curl -x http://user2:pass2@138.2.138.243:3128 https://ifconfig.me
# Должен вернуть IP wwan1
```

## Rollback (если что-то пошло не так)

```bash
# На Orange Pi

# 1. Остановить Python proxy
systemctl stop proxyfarm-proxy

# 2. Запустить Squid обратно
systemctl start squid

# 3. Проверить
curl -x http://proxy:3128 ifconfig.me
```

## Что удалить после успешной миграции

```bash
# Squid больше не нужен
apt remove squid

# Или просто оставить остановленным
systemctl disable squid
```

## Обновление конфига пользователей

### Раньше (Squid):
```bash
# 1. Обновить /etc/squid/passwords
htpasswd /etc/squid/passwords newuser

# 2. Обновить /etc/squid/squid.conf
nano /etc/squid/squid.conf
# Добавить ACL и tcp_outgoing_address

# 3. Reload
squid -k reconfigure
```

### Теперь (Python):
```bash
# 1. Обновить config/proxy.yaml
nano config/proxy.yaml

users:
  newuser:
    password: "newpass"
    modem: "wwan0"

# 2. Hot reload
systemctl reload proxyfarm-proxy

# Или
kill -HUP $(pgrep -f proxy_full.py)

# Готово!
```

## Сравнение производительности

### Squid
- ~50,000 requests/sec
- Для 2 модемов - избыточно

### Python Proxy
- ~5,000 requests/sec (однопоточный)
- ~20,000 requests/sec (с uvloop)
- Для 2 модемов - более чем достаточно

**Пример расчета:**
- 2 модема
- 10 пользователей
- 10 запросов в секунду на пользователя
- = 100 requests/sec общая нагрузка
- Python proxy справится с 5000 req/s - margin 50x!

## Следующие шаги

1. ✅ Миграция на Python proxy
2. 🔄 Добавить Management API для управления пользователями
3. 🔄 Добавить метрики (Prometheus)
4. 🔄 Добавить rate limiting
5. 🔄 Добавить автоматическую балансировку пользователей по модемам

## FAQ

### Q: Можно ли использовать Python proxy и Squid одновременно?
**A:** Да, на разных портах. Squid на 3129, Python на 3128 (или наоборот).

### Q: Нужно ли удалять policy routing (fwmark)?
**A:** Можно удалить, Python proxy не использует fwmark. Но можно и оставить - не мешает.

### Q: Производительность будет хуже?
**A:** Для вашей нагрузки (2 модема, ~10 пользователей) - нет заметной разницы.

### Q: Как добавить больше модемов?
**A:** Просто добавить в `config/proxy.yaml`:
```yaml
modems:
  wwan0: {}
  wwan1: {}
  wwan2: {}  # Новый модем
```

И назначить пользователям: `modem: "wwan2"`
