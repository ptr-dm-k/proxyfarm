# ProxyFarm 🚜

Система управления LTE модемами с HTTP API для ротации IP-адресов и проксирования трафика через мобильные сети.

## Возможности

- 🌐 **HTTP/HTTPS Прокси** через Squid с доступом по VPN
- 🔄 **Ротация IP** для обхода rate limits и блокировок
- ⚖️ **Load Balancing** через multipath routing между модемами
- 📱 **USSD команды** для проверки баланса и управления
- 🔌 **REST API** для управления модемами
- 📊 **Мониторинг** состояния модемов и сети
- 🔧 **Автоматическое восстановление** при сбоях

## Архитектура

```
Клиент → VPS:3128 → [VPN] → Orange Pi → Squid → Multipath Routing → 2x LTE Modems
                                           ↓
                                      FastAPI :8080
```

**Подробнее:** [ARCHITECTURE.md](ARCHITECTURE.md)

## Быстрый старт

### Предварительные требования

- Orange Pi Zero 3 (или аналог)
- 2x LTE модема (USB)
- Ubuntu/Debian Linux
- VPS с OpenVPN (опционально, для публичного доступа)

### Установка на Orange Pi

```bash
# 1. Клонировать репозиторий
git clone https://github.com/yourusername/proxyfarm.git /root/repo/proxyfarm
cd /root/repo/proxyfarm

# 2. Установить зависимости
apt update
apt install -y squid openvpn python3-pip python3-venv \
  modemmanager network-manager curl socat expect

# 3. Установить Python зависимости
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 4. Настроить Squid proxy
./scripts/setup_squid.sh

# 5. Настроить multipath routing (выберите один вариант)

# Вариант A: NetworkManager Dispatcher (рекомендуется)
./scripts/install_nm_dispatcher.sh

# Вариант B: Systemd Service с периодической проверкой
./scripts/install_routing_service.sh

# 6. Проверить настройку
./scripts/check_system.sh  # создайте из TROUBLESHOOTING.md
```

### Настройка VPS (опционально)

Для публичного доступа к прокси:

```bash
# На VPS
git clone https://github.com/yourusername/proxyfarm.git
cd proxyfarm
./scripts/setup_vps_proxy_forward.sh
```

Это настроит:
- OpenVPN server на VPS
- Socat forwarder: VPS:3128 → Orange Pi:3128

## Использование

### Прокси

```bash
# Через VPS (публичный доступ)
curl -x http://138.2.138.243:3128 http://ifconfig.me

# Напрямую (локальная сеть)
curl -x http://192.168.50.111:3128 http://ifconfig.me

# Из Python
import requests

proxies = {
    'http': 'http://138.2.138.243:3128',
    'https': 'http://138.2.138.243:3128'
}

response = requests.get('https://api.ipify.org', proxies=proxies)
print(f"IP: {response.text}")
```

### API

```bash
# Получить список модемов
curl http://192.168.50.111:8080/api/v1/modems

# Получить информацию о модеме
curl http://192.168.50.111:8080/api/v1/modems/0

# Ротировать IP
curl -X POST http://192.168.50.111:8080/api/v1/modems/0/rotate

# USSD команда (проверка баланса)
curl -X POST http://192.168.50.111:8080/api/v1/modems/0/ussd \
  -H "Content-Type: application/json" \
  -d '{"command": "*100#"}'

# Статус прокси
curl http://192.168.50.111:8080/api/v1/proxy/status

# Статус системы
curl http://192.168.50.111:8080/api/v1/system/status
```

**API документация:** http://192.168.50.111:8080/docs (Swagger UI)

### Проверка балансировки

```bash
# Сделать 10 запросов и увидеть распределение IP
for i in {1..10}; do
  curl -s -x http://138.2.138.243:3128 http://ifconfig.me
  echo
done | sort | uniq -c
```

Ожидаемый результат:
```
5 91.151.136.231   # wwan0
5 91.151.137.37    # wwan1
```

## Структура проекта

```
proxyfarm/
├── src/
│   └── proxyfarm/           # Python приложение
│       ├── main.py          # FastAPI app
│       ├── config.py        # Конфигурация
│       ├── api/             # API endpoints
│       ├── core/            # Бизнес-логика
│       └── services/        # Фоновые сервисы
├── scripts/
│   ├── setup_squid.sh                    # Настройка Squid
│   ├── setup_modem_routing.sh            # Multipath routing
│   ├── setup_vps_proxy_forward.sh        # VPS forwarding
│   ├── install_nm_dispatcher.sh          # NM dispatcher установка
│   └── install_routing_service.sh        # Systemd service установка
├── systemd/
│   ├── proxyfarm.service                 # FastAPI service
│   ├── modem-routing.service             # Routing service
│   └── modem-routing.timer               # Routing timer
├── docs/
│   ├── ARCHITECTURE.md                   # Архитектура системы
│   ├── NETWORK_TOPOLOGY.md               # Топология сети
│   └── TROUBLESHOOTING.md                # Решение проблем
├── config.example.yaml      # Пример конфигурации
├── requirements.txt         # Python зависимости
└── README.md               # Этот файл
```

## Документация

- 📖 [Архитектура системы](ARCHITECTURE.md) - подробное описание компонентов, потоки данных, диаграммы
- 🌐 [Топология сети](docs/NETWORK_TOPOLOGY.md) - схемы сети, IP адреса, маршрутизация
- 🔧 [Устранение неисправностей](docs/TROUBLESHOOTING.md) - решение типичных проблем
- 📚 [Использование прокси](PROXY_USAGE.md) - примеры для разных языков

## Технологии

### Системные компоненты
- **ModemManager** - управление LTE модемами
- **NetworkManager** - управление подключениями
- **Linux Kernel Routing** - multipath балансировка
- **Squid 6.13** - HTTP/HTTPS прокси
- **OpenVPN** - защищенный VPN tunnel

### Python стек
- **FastAPI** - современный async веб-фреймворк
- **Pydantic** - валидация данных
- **asyncio** - асинхронные операции
- **httpx** - HTTP клиент

## Ключевые особенности

### Multipath Routing

Система использует Linux kernel multipath routing с L4 hash policy для балансировки трафика:

```bash
# Multipath route через оба модема
default
    nexthop via 10.231.254.1 dev wwan0 weight 1
    nexthop via 10.223.35.1 dev wwan1 weight 1

# WiFi как backup
default via 192.168.50.1 dev wlan0 metric 1000
```

**Алгоритм балансировки:**
- Hash по: source IP, dest IP, source port, dest port
- Одно TCP соединение всегда через один модем (нет разрывов)
- Разные соединения распределяются между модемами

### Автоматическое поддержание маршрутов

Два подхода на выбор:

1. **NetworkManager Dispatcher** (рекомендуется)
   - Реагирует на события сети мгновенно
   - Восстанавливает маршруты при любых изменениях

2. **Systemd Timer**
   - Проверяет маршруты каждые 5 минут
   - Проще для отладки

### IP Rotation

API для ротации IP адресов:

```bash
POST /api/v1/modems/{id}/rotate
```

**Процесс:**
1. Сохранить текущий IP
2. Отключить модем
3. Подождать disconnect
4. Подключить модем
5. Дождаться нового IP
6. Обновить маршруты
7. Перенастроить Squid

**Время ротации:** 10-30 секунд

## Производительность

- ⚡ **Латентность:** 1-3 секунды на запрос через прокси
- 🔄 **Балансировка:** ~50/50 между модемами
- 📊 **Throughput:** Зависит от скорости LTE (обычно 10-50 Mbps на модем)
- 💪 **Надежность:** Автоматическое восстановление при сбоях

## Мониторинг

```bash
# Системный статус
curl http://192.168.50.111:8080/api/v1/system/status

# Health check
curl http://192.168.50.111:8080/api/v1/health

# Логи Squid
tail -f /var/log/squid/access.log

# Логи FastAPI
journalctl -u proxyfarm.service -f

# Системные логи
journalctl -f | grep -E "wwan|NetworkManager|ModemManager"
```

## Отладка

```bash
# Проверить routing table
ip route show

# Проверить статус модемов
mmcli -L
mmcli -m 0
mmcli -m 1

# Тестировать каждый интерфейс
curl --interface wwan0 ifconfig.me
curl --interface wwan1 ifconfig.me

# Тестировать default route
curl ifconfig.me

# Запустить полную диагностику
./scripts/check_system.sh
```

**Больше информации:** [Troubleshooting Guide](docs/TROUBLESHOOTING.md)

## Известные проблемы

### ⚠️ Прокси возвращает WiFi IP вместо модемного

**Статус:** 🔄 В процессе решения

**Проблема:** NetworkManager восстанавливает WiFi default route с приоритетом выше multipath route.

**Решение:** Установить NetworkManager Dispatcher или Systemd Timer для автоматического поддержания правильных маршрутов.

```bash
# Вариант A (рекомендуется)
./scripts/install_nm_dispatcher.sh

# Вариант B
./scripts/install_routing_service.sh
```

## Roadmap

- [x] Базовое управление модемами через API
- [x] Ротация IP
- [x] Squid proxy с VPN доступом
- [x] Multipath routing
- [x] VPS forwarding
- [ ] Автоматическое поддержание маршрутов (в тестировании)
- [ ] Фоновый мониторинг состояния
- [ ] Автоматическое восстановление при сбоях
- [ ] Метрики и статистика
- [ ] Web UI для управления
- [ ] Docker контейнеризация
- [ ] SOCKS5 прокси
- [ ] Ротация по расписанию
- [ ] Webhook уведомления

## Вклад

Pull requests приветствуются! Для крупных изменений сначала откройте issue для обсуждения.

## Лицензия

MIT

## Автор

Petr - [GitHub](https://github.com/yourusername)

## Поддержка

- 📧 Email: your.email@example.com
- 💬 Telegram: @yourusername
- 🐛 Issues: [GitHub Issues](https://github.com/yourusername/proxyfarm/issues)

---

**⚠️ Disclaimer:** Используйте эту систему ответственно и в соответствии с законодательством вашей страны и условиями использования мобильного оператора.
